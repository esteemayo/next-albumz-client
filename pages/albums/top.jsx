import styles from '@/styles/TopAlbum.module.scss';

const TopAlbums = () => {
  return (
    <section className={styles.container}>
      <h1>Top 10 Albums</h1>
    </section>
  );
};

export default TopAlbums;
